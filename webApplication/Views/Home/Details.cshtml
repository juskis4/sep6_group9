@model webApplication.ViewModels.MovieViewModel

<!-- Layout and other HTML -->

<div class="container">
    <h2>@Model.Title</h2>
    <p>@Model.Year</p>
    <p>@Model.Rating?.Value</p>
    <!-- Display other movie details here -->
</div>

<!-- More HTML -->